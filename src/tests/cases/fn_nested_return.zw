fn nested(a) {
    if (a > 3) {
        if (a < 5) {
            return a;
        }
    }
    {
        a = a + 2;
        return a;
    }
    return -1;
}

println(nested(5));
println(nested(1));
println(nested(4));